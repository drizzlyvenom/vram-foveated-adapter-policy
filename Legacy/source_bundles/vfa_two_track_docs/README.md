# VFA Two-Track Documentation Bundle

Read `CODEX_README.md` first.

This bundle reframes the project around:

1. shared-backbone LoRA specialist consolidation, and
2. FoveateR-style visual evidence compression.

Copy the `docs/` directory and `CODEX_README.md` into the repository root, then let Codex read the markdown files.
