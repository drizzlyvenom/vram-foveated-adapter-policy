PyYAML>=6.0
requests>=2.31
Pillow>=9.0
numpy>=1.23
accelerate>=1.0
qwen-vl-utils>=0.0.8
safetensors>=0.4
transformers>=4.51
peft>=0.11

# Install CUDA PyTorch separately for the target machine.
# Example: use the official PyTorch wheel index matching the local CUDA runtime.
